name: navigation
version: 0.0.1

environment:
  sdk: ^3.6.0
  flutter: ">=1.17.0"

dependencies:
  flutter:
    sdk: flutter

  core:
    path: ../core
  home_page:
    path: ../features/home_page
  auth:
    path: ../features/auth
  character_page:
    path: ../features/character_page
  settings_page:
    path: ../features/settings_page
  user_profile:
    path: ../features/user_profile

  auto_route: ^9.3.0+1

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0